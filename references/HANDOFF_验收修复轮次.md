# Handoff：验收—修复循环的接力棒（新 session 冷启动读这一份）

> **用途**：方案C 阶段2/3 已全部落地，项目进入**「用户在 Word 里验收 → 报问题 → 修 → 再验收」**
> 的循环。这份文件给接手的新 session：现在做到哪、**还没确认的第一优先事项是什么**、怎么复现、
> 以及哪些坑绝对不能踩回去。
>
> **一句话**：困扰四轮的两件大事——「打开成品 Word 提示『发现无法读取的内容』」与「二/三/四级标题
> 全局编号」——已在第五轮验收中被用户**确认修复**。第五轮的三项排版细节（制表符、红色斜体、"附件"
> 参与编号）与第六轮的四项（空行样式、一/二/四级**序号字体**、二级**半角括号**）均已修复且
> **用户已确认通过**。第七轮：新报两项（**手写序号后没有制表符**、**自检因原件自带的元素乱序退 2**）
> 已修复，另有一条**裁决反转**——**手写序号一律转成 Word 自动编号、并删掉文字里的手写序号**；
> 连同"封面空行也套正文样式"一起**待下一轮确认**，见 §3。

分支：`claude/skill-style-fixes-xiqr0r`　　回归：**140 passing**
（`pip install lxml` 后 `python3 -m unittest discover -s tests -p "test_*.py"`）

---

## 0. 三分钟上手

```bash
pip install lxml
python3 -m unittest discover -s tests -p "test_*.py"     # 应 140 passing
python3 scripts/make_canonical_reference.py /tmp/ref.docx # 人肉验收参考件（可选）
```

**必读顺序**：本文件 → `references/已知陷阱与设计决策.md` 的**索引表**（只看表，按需展开条目）
→ `CLAUDE.md`（开发约定）。`SKILL.md` 是运行工作流的权威。**别整篇读陷阱文件**，20 条里单次任务
通常只需 1–2 条。

**架构一句话**：判定层（`scripts/lib/checks.py`，纯函数出 `fixes.json`）与应用层
（`scripts/40_apply_fixes.py`，改 XML）分离；规范值全部来自 `spec/format_spec.json`；
canonical 样式与 canonical 编号定义的**唯一定义**在 `scripts/lib/canonstyles.py`（参考件生成器与
流水线共用同一份字符串）。

---

## 1. ✅ 已了结：Word「发现无法读取的内容」（第五轮用户确认不再报错）

连续三轮的头号问题，第五轮验收用户确认**成品打开不再报错**。下面留作**回归清单**——这些坑都曾真实
发生过，改动 apply 层时对照着看，别踩回去。**沙箱里没有 Word，这类结论永远只能靠用户实测**（见 §4）。

已修的成因（全部属于「**良构 ≠ 合法**」——XML 解析没问题、zip 也完好，但 Word 拒绝打开）：

| # | 成因 | 状态 |
|---|---|---|
| 1 | `<w:suff>` 写在 `<w:numFmt>` 之前（违反 CT_Lvl 的 sequence） | 已修（第二/三轮） |
| 2 | `<w:jc>` 在 `<w:ind>` 前、`<w:outlineLvl>` 在 `<w:spacing>` 前、往只有 `<w:sz>` 的 Normal 里 append `<w:rFonts>` | 已修 |
| 3 | `docGrid` append 到 `printerSettings` 之后、`updateFields` 塞到 settings 最前、`w:num` 落到 `numIdMacAtCleanup` 之后 | 已修 |
| 4 | **样式重名**（注入的"标题 1"/"图标题"与模板已有样式撞车） | 已修：样式名加 `FGW` 前缀 + 注入时兜底改名 + Normal 按「默认样式 → styleId → **名字**」三级查找 |
| 5 | **批注被覆盖写**：原有批注被抹掉、document.xml 里它们的标记变成**悬空引用**，新批注 id 从 0 重排**撞 id** | **第四轮新修**（陷阱 #20）——这是本轮唯一**可复现、已量化**的成因：对成品再跑一遍 → 14 处悬空引用 + 9 处重复 id |
| 6 | **编号克隆复制了源身份**：两条 `abstractNum` 抢同一个 `styleLink`、同一批级别 `pStyle` | **第四轮新修**（陷阱 #15 补充） |

**若再次出现同类报错**（不要先猜，先取证）：

```bash
python3 scripts/diagnose_docx.py <用户手上那个报错的成品.docx>
python3 scripts/diagnose_docx.py <对应的原始文档.docx>     # 对照：原件本来干不干净
```

`diagnose_docx.py` 是**只读**排查工具，专查 XML 良构检查发现不了的那类损坏：元素顺序违反 sequence、
样式名/styleId 重复、`pStyle`/`numId`/关系 `r:id` 悬空、缺 Content-Type，**以及本轮新增的批注一致性
（悬空引用/重复 id/区间不配对）与编号身份链接重复**。输出 JSON、退出码 2 表示有问题。
**两个文件都要查**——如果原件本来就带问题，那就不是我们引入的。45 步已把这两项新检查也纳入硬失败，
并且**自己就会跟原件对照**（第七轮起）：原件本来就有的同类问题只进 `warnings`，不再挡住交付。

若 `diagnose_docx.py` 报 **clean 但 Word 仍报错**：把成品在 Word 里「另存为」后再 `diagnose_docx.py`
对比两份的差异（Word 修复时会重写它认为非法的部分），差异处即真凶。

---

## 2. 已实现的功能全景（都已跑通并有测试锁定）

**方案C 阶段2/3（样式注入）**——规范值不再写成段落直接属性，而是由**注入的 canonical 命名样式**
承载，段落只指派 `pStyle` 并清掉样式已承载的直接属性。45 步有**全坍缩不变量**兜底（属性仍由
`direct`/`numbering` 供给且值不合规即硬失败退 2）。契约细节见陷阱 #12。

七轮验收累计落地：

- **文档网格**每页 41 行 / 行距 15.6 磅（`docGrid` + settings 的 compat 块 + `Normal`=五号）；
- **目录**制表位按字符数写进 TOC 样式（点线、页码不换行）——已通过验收；
- **首行缩进**只写字符单位，Word 显示"2 字符"——已通过验收；
- **一~四级标题加粗**，加粗由样式承载（连 run 上的直接加粗、显式取消加粗、抢戏的字符样式一起清）；
- **标题/图表标题删首尾空格**；
- **图/表标题改用 Word 自动编号**（`图%1`/`表%1`、`suff=tab`），编号**挂在样式上**——已通过验收；
- **标题自动编号改挂注入的四级 canonical 列表**（第四轮，已确认编号正常），**每级序号自带字体**
  （第六轮：一级黑体 / 二级半角括号 TNR / 三级数字 TNR / 四级仿宋）；**手写序号一律转成自动编号、
  文字里的序号删掉**（第七轮，方案一作废），不转的四类保留手写序号并补真 `w:tab`；
- **空行**一律套正文样式（第六轮：**封面空行也套**，例外只剩目录域跨度内）；**图片/文本框段落**不套
  （固定行距会把图裁成一行）；
- **表格**默认值（`tblInd`/`tblCellMar`/`vAlign`）；
- **批注合并**进文档已有批注（第四轮，已确认不再报文档损坏）。

---

## 3. 本轮（第七轮）修掉、**待下一轮确认**的三项 + 一项追加裁决

0. **【裁决反转】手写序号一律转成 Word 自动编号**（用户："我希望手写序号也转变为自动编号，但要注意去掉
   原本的手写编号，这里逻辑需要缜密一点"）。第四轮的方案一（文字里有可选中的序号＝手动序号、取消该段
   自动编号）**作废**。现在已确认标题一律：把文字里的序号连同其后的分隔空白/`w:tab` 删掉 → 段落挂到
   canonical 四级列表（`checks.continuity` 出 `autonumber_heading`，`40._apply_autonumber_heading` 执行）。
   **缜密之处**（改这块前先读陷阱 #19 的第七轮补丁，五条一条都不能少）：删与挂**同生共死**（任一步失败
   就整条放弃、停在"手写序号 + 无自动编号"的自洽状态）；**四类不转**（pattern 未确认标题 / 惯例不编号
   章节 / 表格里的标题 / 整段只有序号没有标题文字的）；只在**确实有事可做**时出 fix（否则每轮重复报）；
   删序号要**同时**吃掉后面的空白与 `w:tab`；`_blank_style` 跳过"空文字但有自动编号"的段落。
   **验收看**：所有正文标题的序号都是 Word 生成的（点进去光标选不中序号），层级各自连贯、插入一章后
   自动重排；"结论/参考文献/附录"等仍不带编号；文字里不再有残留的手写序号或多余空格/制表符。
   **首轮会给每个转换过的标题挂一条批注**（说明"原手写序号已删除"），再跑一遍就安静了。

1. **手写序号的标题，序号后没有制表符**（"换一个自动编号的文档就有"）。**注意**：本轮 0 号裁决之后，
   这条只对"保留手写序号"的那四类还起作用；普通标题的制表符改由编号定义的 `suff=tab` 供给。
   两条路径此前各自为政：
   自动编号的制表符来自编号定义的 `suff=tab`，而序号手写在文字里的标题走文本路径，
   `renumber_heading` 只把序号归位成规范 token、分隔符原样保留。现在判定层按
   `spec.heading_numbering.suffix` 多出一条 `set_number_tab`（`checks._check_number_tab`），应用层
   `_heading_number_tab` 把序号处的 `w:t` 一切为二、中间插一个**真正的 `w:tab` 元素**，并清掉原有的
   空白/制表符。两个配套点：抽取阶段单独记 `num_tab`（`para_text` 看不见制表符，不记就每轮重复补）；
   实际改动**推迟到所有 fix 之后**（renumber 会把文字并回第一个 `w:t`，把制表符甩到文字尾巴上）。
   **验收看**：手写序号的文档里，一~四级标题都是「序号 + 制表符 + 标题文字」，与自动编号的文档一致。
2. **45 自检退 2，报 `numbering.xml` 里 `rPr` 的 `shadow` 在 `specVanish` 之后，但文档能正常打开**。
   那处乱序**原件本来就有**（WPS/LibreOffice 转换件常自带），不是本次引入的，却被自己的自检挡住交付。
   现在 45 把元素顺序、批注一致性、编号身份三项在**产物**与**原件**上各跑一遍，原件已有的进
   `warnings` + `element_order_preexisting` / `structural_preexisting`（不阻断），只有新引入的才退 2。
   比较**按签名不按条数**（克隆钳会复制模板的 `abstractNum`，按条数扣会把复制件误判成新引入）。
   **验收看**：同一份文档再跑一遍，`45` 退 0，`validate_report.json` 里能看到那条 warning。
3. **追加裁决：封面空行也套正文样式**（用户第六轮末追加，推翻此前"封面留白不动"）——见下面 §3.1 的
   第 1 条，封面留白因此按正文的固定行距 28 磅算高度、比原先更高。

---

## 3.1 上一轮（第六轮）修掉、用户已确认的四项

演示时新发现的四个问题，全部落在「空行样式」与「标题序号字体」两块：

1. **空行没有应用正文样式**（含封面）。根因不是规则没实现，而是 `_blank_style` 的**目录例外太宽**：它用
   `is_toc`，而 `is_toc` 含"只是套着目录样式"的段落——目录后面那几个顺手继承 `TOC3`/`Contents 3`
   样式的空行并不在目录域里，却被一起放过。现在 20 单独记 `toc_in_field`（真在 TOC 域/内容控件
   跨度内），例外只认它。**同轮用户追加裁决：封面空行也要套正文样式**（推翻此前"封面留白不动"
   的约定），`_blank_style` 里的 cover 例外已整条删除——代价是封面留白按正文的固定行距 28 磅算
   高度、比原先的五号行高更高，用户知情。**验收看**：封面、目录之后、正文里的空行点开都是
   `FGW正文`（仿宋三号）；只有目录域跨度内的空行仍不动（刷新目录会重写它们）。
2. **一级标题序号要与标题内容同为黑体**；3. **二级标题前后要英文括号、字体 Times New Roman**；
   4. **四级标题序号要仿宋**。三件事同一个根因：清干净段落标记后，序号完全跟随**标题样式**渲染，
   于是数字/半角括号落到样式的西文位（Times）、全角括号落到中文位。现在**每个编号级别自带
   `rPr`**（＝Word「定义多级列表 → 字体」的落点，取自 `spec.heading_numbering.levels.<级>`）：
   一级黑体、二级楷体+括号 TNR、三级仿宋+数字 TNR、四级整体仿宋；二级的 `lvlText` 与
   `checks._heading_token` **一起**改成半角 `(一)`。**验收看**：点在序号上看 Word 字体框——
   一级黑体、二级括号 TNR（括号内中文楷体）、四级整个「（1）」仿宋；二级序号显示为 `(一)`。
   **已知残留**：手写在文字里的四级序号「（1）」，其中的**数字**仍随标题样式走 Times New Roman
   （文字里的西文由「西文＝TNR」这条全局规范承载，样式管不到"只有这个数字例外"）。若这也要改成
   仿宋，得给序号那截文字开一个直接格式的口子，并同步放宽 45 的全坍缩不变量——先请用户确认再动。

---

## 3.2 第五轮修掉、用户已确认的三项

1. **标题序号后的制表符**（用户："一/二/三/四级标题序号后的制表符没有了"）。上一轮把 canonical 标题
   编号的 `suff` 按"与手写序号同形"的直觉写成了 `nothing`/`space`，抹掉了阶段0 验收过的制表符排版。
   现在 `suff=tab`（取自 `spec.heading_numbering.suffix`），落点仍是 `defaultTabStop`＝2 字符。
   **验收看**：序号与标题文字之间有制表符对齐，不是紧贴/单个空格。
2. **部分标题序号是红色斜体**。自动编号的渲染取自**段落标记的 `rPr`**：模板在标记上留了斜体/颜色/
   字符样式，序号就红且斜，而标题文字是黑色正体。现在指派 canonical 样式时把段落标记上的加粗、
   全部装饰性属性（`_MARK_DECORATION_TAGS`）与**任何** `rStyle` 一并清掉。**验收看**：所有层级的序号
   与其标题文字同字体、同颜色、无斜体。
3. **「附件」允许自动编号**（用户裁决）：报告里的"附件"常常就是正文的一章。已从
   `checks.UNNUMBERED_HEADING_PREFIX` 移出（**「附录」仍在**）。**验收看**：挂了标题样式的"附件"带序号，
   且它占用同级序号位（后面的同级标题顺延）。

---

## 4. 铁律与长期约束（别踩）

- **红线**：只做格式、**不改原文**、判定值**只来自 spec**、**不手写 `fixes.json`**。
- **沙箱没有 Word / soffice**：渲染类结论**只能靠用户实测**。凡是"Word 里看起来如何"的判断，
  写代码时就要想好怎么让用户一眼验证，别自己拍脑袋下结论。
- **良构 ≠ 合法**：`45` 的 XML 良构检查发现不了元素顺序、样式重名、悬空批注这类问题。往 XML 里插元素
  一律走 `_get_or_make`（自动查 `docxcommon.ELEMENT_ORDER` 按序插），**别 append 了事**；新增一类
  结构性损坏时，把探测器写进 `docxcommon` 并让 45 与 `diagnose_docx.py` **共用**。
- **写任何"部件级"内容前先读已有的**（批注是血的教训 #20）：文档里可能已经有同类内容，覆盖写等于
  改原文 + 留下悬空引用。
- **安全阀**（陷阱#5）：仅凭形状认出、没有样式/大纲级别撑腰的标题与图表标题（`pattern`），
  **绝不自动改文字**，也不指派 canonical 样式（否则下一轮它会凭样式"转正"、绕过安全阀）。
- **共享对象不可原地改**：`abstractNum`、字符样式都可能被多段共享，要改就**克隆**或**摘引用**，
  原地 mutate 会溢到无关段落（老 bug #17）；**克隆还必须摘掉源身份**（#15）。
- **同一规则只写一份**：canonical 样式/编号、区域划分、字符样式判断、结构性探测器都已收敛到共享模块。
  再出现"两处各写一遍"必然漂移——这是本项目栽过最多次的地方。

---

## 5. 代码地图（动手前先定位）

| 关注点 | 位置 |
|---|---|
| canonical 样式 + canonical 编号定义（角色→样式、`governs` 表、标题/图表编号） | `scripts/lib/canonstyles.py` |
| 判定层（出 fixes，**架构中性、尽量别动**） | `scripts/lib/checks.py`（`paragraph_role` 是判定与指派的共用分派） |
| 应用层（注入/指派/清覆盖/编号/网格/表格） | `scripts/40_apply_fixes.py` |
| 元素顺序表、字符样式判断、cascade、**结构性探测器**（批注/编号身份） | `scripts/lib/docxcommon.py` |
| 批注写入（**合并**，不覆盖） | `scripts/lib/commentwriter.py` |
| 产物自检（含元素顺序、批注一致性、全坍缩不变量；结构性问题先与原件对照） | `scripts/45_validate_output.py` |
| 任意 docx 的损坏排查 | `scripts/diagnose_docx.py` |
| 人肉验收参考件生成器 | `scripts/make_canonical_reference.py` |
| 规范值（唯一权威） | `spec/format_spec.json`；人类可读镜像 `references/format_spec.md` |

---

## 6. 维护提醒

- `references/已知陷阱与设计决策.md` 现有 **20 条**，超出它自己定的"约 10 条以内"。下次改动时
  顺手审一遍哪些可以退役（判据是：决策被推翻、或对应代码已删除）。**别为了压条数删掉仍然生效的
  条目**——那些是防回退的唯一记录。
- 每落一个改动，**同步** `SKILL.md` / `references/format_spec.md` / spec 的 `source` 描述，
  并给"别回退"级别的决策补一条陷阱记录 + 一个锁住它的测试（CLAUDE.md 的硬要求）。
- 历史 handoff：`HANDOFF_方案C实施.md`（方案C 背景与契约）、`HANDOFF_阶段2_样式注入.md`
  （阶段2 实施记录）、`HANDOFF_格式化架构讨论.md`（早期技术根因与 17 项分诊）。
  都是**背景资料**，现状以本文件为准。
